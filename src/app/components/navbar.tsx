const Navbar = () => {
    return (
        <div className="navbar bg-base-100 shadow-sm">
            <button className="btn btn-ghost text-xl">Cameelers</button>
        </div>
    );
};

export default Navbar;
